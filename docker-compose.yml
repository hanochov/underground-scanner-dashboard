version: "3.8"

services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server:/app
    depends_on:
      - client

  client:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
